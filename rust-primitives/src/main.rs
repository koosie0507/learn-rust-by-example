fn main() {
    let logical: bool = true;
    
    let a_float: f64 = 1.0;
    let an_integer = 5i32;

    let default_float = 3.0;
    let default_integer = 6;

    let mut inferred_type = 12;
    inferred_type = 429291929191i64;

    let mut mutable = 12;
    mutable = 21;

    let mutable = true;

    let unit = ();

    let arr = [1,2,3];
    let tuple = (1, "abc");
}
